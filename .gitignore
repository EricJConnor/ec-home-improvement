node_modules/
.next/
out/
build/
.env
.env*.local
.vercel
.DS_Store
*.pem
npm-debug.log*
.work/
next-env.d.ts
tsconfig.tsbuildinfo

# The hero footage IS committed: Pexels is unreachable from CI, so it cannot be
# refetched, and Vercel builds from the repo. Only the preview cut is derived.
public/video/hero-preview.mp4
